<html>
<body>
<head>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
</head>
<div class="container">
    <h3>Ajouter un utilisateur à l'évènement "<%= libelle %>"</h3>

    <!-- liste des utilisateurs potentiels -->
    <form id="formEvent" >
        <div class="form-group">
            <label for="exampleFormControlSelect2" >Utilisateur</label>
                <select multiple class="form-control" id="selectList" name= "idUser"  >
                    <!<% for(var i=0; i<collectionUser.length; i++) {%>
                        <option value = "<%= collectionUser[i].id %>"><%= collectionUser[i].first_name %><p> </p><%= collectionUser[i].last_name %></option>
                    <% } %>
                </select>
        </div>
        <!-- Fin liste utilisateurs-->
        <div class="row">
            <p class="col-6"></p>
            <button type="submit" class="col-2 btn btn-primary btn-sm" >Valider</button>
            <p class="col-1"></p>
            <button class="col-2 btn btn-primary btn-sm" ><link href="/">Annuler</button>
            <p class="col-1"></p>
        </div>

    </form>
</div>
</body>
<script>
  const TheForm = document.getElementById("formEvent");
  const myUrl = "/event/<%= idEvent%>/token";
  //ligne a effacer??
  const sb = document.querySelector('#selectList')

  TheForm.addEventListener("submit", (e) =>{

    //annule la prise en main vers une url impossible à manipuler
    e.preventDefault();

    const requete = new XMLHttpRequest();
    requete.open("post" ,myUrl);
    requete.onload = function (){

      window.location.replace("/event/list");
    }
    const paramToDisplay = new FormData(TheForm);
    requete.send(new FormData(TheForm));


  });
</script>
</html>
